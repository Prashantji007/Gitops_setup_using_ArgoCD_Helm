apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-get-task-svc
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}-get-task
  ports:
  - port: {{ .Values.get_task.serviceport }}
    targetPort: {{ .Values.get_task.port }}
