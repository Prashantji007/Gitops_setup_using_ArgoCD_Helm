apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-delete-task-svc
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}-delete-task
  ports:
  - port: {{ .Values.delete_task.serviceport }}
    targetPort: {{ .Values.delete_task.port }}
