apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-ui-svc
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-todo-ui
  ports:
  - port: {{ .Values.microui.serviceport }}
    targetPort: {{ .Values.microui.port }}
